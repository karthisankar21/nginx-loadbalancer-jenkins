# Force LF for shell scripts (Jenkins + WSL safe)
*.sh text eol=lf

# Force LF for Linux config files
*.yml text eol=lf
*.yaml text eol=lf
*.env text eol=lf
Dockerfile text eol=lf

# Keep Windows batch files in CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# Normalize all other text files to LF in repo, auto-convert on checkout
* text=auto